---
export type Props = {
  [key: string]: string | undefined;
};

const { label, ...rest } = Astro.props;
---

<button
  class="text-gray-700 px-3 py-2 rounded-md bg-gray-100 hover:bg-gray-300"
  {...rest}><slot /></button
>
