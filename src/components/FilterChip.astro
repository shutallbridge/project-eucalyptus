---
import { Icon } from "astro-icon/components";

export type Props = {
  [key: string]: string | undefined;
};

const { ...rest } = Astro.props;
---

<!-- 
.filter-chip {
  /* base styles */
  display: flex;
  border-radius: 0.375rem; /* md */
  overflow: hidden;
  color: #404040; /* gray-700 */
}

.filter-chip__label {
  padding: 0.5rem 0.75rem; /* py-2 px-3 */
  background-color: #f5f5f5; /* gray-100 */
}

.filter-chip__button {
  padding: 0.5rem 0.75rem;
  background-color: #f3f4f6;
  border-left: 1px solid #e5e5e5; /* gray-200 */
}

.filter-chip__button:hover {
  background-color: #d4d4d4; /* gray-300 */
}

the code below uses invalid css classes to demonstrate the styles above but does not affect the styling of the component
-->
<div class="filter-chip text-gray-700 rounded-md overflow-hidden flex">
  <span class="filter-chip__label py-2 px-3 bg-gray-100"><slot /></span>
  <button
    class="filter-chip__button py-2 px-3 border-l border-gray-200 bg-gray-100 hover:bg-gray-300"
    {...rest}
  >
    <Icon name="heroicons:x-mark-20-solid" size="20" />
  </button>
</div>
